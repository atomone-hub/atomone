syntax = "proto3";

package atomone.consensus.module.v1;

import "atomone/app/v1alpha1/module.proto";

// Module is the config object of the consensus module.
message Module {
  option (atomone.app.v1alpha1.module) = {
    go_import : "github.com/atomone-hub/atomone/x/consensus"
  };

  // authority defines the custom module authority. If not set, defaults to the
  // governance module.
  string authority = 1;
}
